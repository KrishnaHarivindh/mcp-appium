# Contributing

Use focused branches, clear commit messages, and pull requests with a summary, test notes, and screenshots when UI behavior changes.

Commit style: `feat:`, `fix:`, `docs:`, `test:`, `ci:`, `security:`.
